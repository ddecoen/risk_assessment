import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "GAAP Audit Risk Assessment",
  description: "First Annual GAAP Audit Risk Assessment Dashboard - Revenue Recognition, Materiality, COGS Allocation, Segregation of Duties, and Supervision & Review",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}